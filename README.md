# Starting the server
```
docker-compose up -d
```

# Testing server endpoints
```
curl 127.0.0.1/index
```

# TODO list
- Switch to typescript? Just to try it out?
- Allow dynamic bus stop addition / removal
- Make it work for all screen sizes (desktop to phone)